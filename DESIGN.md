# DESIGN.md — dsh-data-mining 设计基线

> 这份文档是我们讨论的成果，是**基线**：以后每次讨论、每次加功能、每次改动，都拿它来对照——"这个改动符不符合我们的设计？"。改这份文档本身也算设计决策，要讨论后再改。

## 一、这个插件是干什么的（一句话）

一个**全程协作**的表格数据挖掘插件：人定方向和规则，agent 干活；容易出错的机械环节用工具卡死；所有决定记下来。

## 二、核心思路（大白话版）

1. **人定方向，agent 干活。** 目标、指标、业务含义这类"答案在人脑里"的事，人说了算；探索、写脚本、清洗、建模这类"答案在数据里、在代码里"的事，agent 全权。
2. **机械环节用工具卡死。** 切分、防泄漏这类"机械可判定对错、但靠自觉必翻车"的事，不能让 agent 自己保证，要用工具强制，错了就拒绝。
3. **所有决定记下来。** 目标是什么、切分怎么切、age 列的 999 怎么处理、试过什么模型——全部记录成结构化文件（manifest），让后面每一步能引用，让最终结果可复现。

## 三、为什么普通 agent 做不好数据挖掘（三个结构性缺陷）

模型的知识没问题（它懂 CRISP-DM、懂 sklearn、懂泄漏），缺的是结构：

| 缺陷 | 大白话 | 后果 |
|---|---|---|
| 没有先验，探索无方向 | 专家带着业务直觉直奔风险所在；agent 要么乱撞要么穷举 | 烧上下文、漏关键问题 |
| 没有持久记忆 | 专家脑子里的"数据集地图"；agent 的记忆就是此刻的上下文，被工具结果冲来冲去 | "我们决定过 X"答不上来，重复劳动 |
| 没有反馈回路 | 开放任务没有标准答案，agent 算错、泄漏、丢行，输出都"看起来正常" | 它不知道自己错了，自信地给出假指标 |

所以：**这个插件是结构插件，不是知识插件。** 插件不需要自带"什么是 CRISP-DM、什么是泄漏、sklearn 怎么用"这类通用知识——模型训练时已经见过海量教程，它缺的是结构不是知识。让机械错误变安静的功能要警惕；增加探索成本的功能要砍。

这里要分清两种"教"：**用户教 agent 业务知识**（流失指什么、`income` 是家庭收入）是协作的核心机制，完全应该；**插件教 agent 通用数据挖掘知识**才是低价值。技能里真正起作用的是"协议成分"（何时停下来问人、报指标必须带基线对比、测试集只碰一次、每个决定记下来）——这是我们的机制，模型脑子里没有；而"知识成分"（什么是泄漏）模型都会，保留精简版当唤起提醒即可，不当重点。

## 四、六步流程：每步的主要问题、分工、机制

每一步都按"专家怎么做 → 普通 agent 哪里会栽 → 我们拿什么补"推导。

### 第 1 步 业务理解

- **主要问题**：把散乱的业务知识采集成硬约束。目标是预测、归因还是分群？"流失"的定义（连续 90 天无订单？沉默算不算？）？成功指标（漏报 vs 误报的代价）？哪些字段是"事后才知道的"（收了就是泄漏）？交付形式？
- **人机分工**：这些答案全在人脑里，人答。agent 负责**问**。
- **普通 agent 的失败**：不问就冲（默认"训练一个模型，用 accuracy"）；或散问一堆、关键问题被淹没。
- **机制**：**最小问题集**——固定的一组问题（目标/标签定义/成功指标/未来信息边界/约束/交付形式），一次问完、每问必答；答案**结构化落盘进 manifest**，成为后面每一步的裁判依据。人设写死：目标未经确认，不得进入数据。

### 第 2 步 数据理解

- **主要问题**：画出"数据集地图"。两件事：**数据质量**（缺失/离群/重复/不一致，哪里不可信）和**业务语义**（每列是什么角色：粒度/目标列/标识符/特征候选/事后信息/噪音；有没有时间结构；目标是否不平衡）。
- **人机分工**：数据能证明的（分布、缺失模式、关联、失衡比例）agent 全权；数据证明不了的（`income` 是个人还是家庭收入？聚合键选哪个？预测时点？`age=999` 的成因？）人补。
- **普通 agent 的失败**：用 `cat`/`head` 看数据，分布/缺失/失衡根本看不见，"看起来没问题"；自己算统计会算错；无方向地乱看烧上下文。
- **机制**：**看数据的工具**，且要"给解释不给裸数字"——profile 直接说"此列右偏、3 个离群值、max 疑似哨兵值 999"，让 agent 第一次看数据就带着"这里有异常"的信号。这一步的交付物：数据地图 + 特征假设清单 + 疑点清单，进 manifest。

### 第 3 步 数据收集

- **主要问题**：盘点源、粒度对齐、时间边界、范围控制。电商场景里购买记录/商品信息/评价是三张表，怎么拼到分析粒度？预测时点之后的信息绝不能收（泄漏的第一入口在收集，不在预处理）。
- **人机分工**：收什么、边界在哪（聚合键、预测时点、哪些字段不收）人定；怎么收（写 join/聚合/抽样脚本）agent 全权，用 bash 自己写。
- **普通 agent 的失败**：不知道要多表 join；join 键用错、粒度没对齐；把未来字段收进来；一上来搬全量数据。
- **机制**：**盘点工具**（扫工作区找数据文件：格式/大小/行数）+ **边界声明协议**（聚合键/时间边界/不收字段进 manifest）。**不做**"帮你收集"的工具——那太厚，agent 用 bash 写脚本更好。

### 第 4 步 数据清洗

- **主要问题**：纯统计信号处理——离群（IQR/z-score）、精确重复、缺失、类型修正。不涉及业务规则（刷单、测试数据那类属于收集环节的边界管理，不在本插件范围）。
- **人机分工**：agent 全权。但**统计决策要记录**（离群阈值、缺失处理策略：drop/impute/flag），决策进 manifest。
- **普通 agent 的失败**：静默删行删列不留记录；"先全量清洗再切分"——统计判定的阈值（IQR 边界、重复判定）用全量数据的分布定，本身就是软泄漏。
- **机制**：**决策清单**（逐列记录怎么处理、为什么，供人审阅）+ 顺序纪律（统计决策在训练集内定）。

### （切分，在清洗之后、预处理之前）

- **主要问题**：切分先于一切"从数据里学参数"的变换。分层（分类）、时序（按时间）、分组（按实体）。
- **人机分工**：agent 执行。
- **普通 agent 的失败**：随机切时序数据、不按实体分组切、重复行跨 train/test、全量 fit scaler 再切分（泄漏，指标假得漂亮）。
- **机制**：**强制工具** `split_dataset`——执行切分并把**切分元数据**（策略、行号、hash、组键、时间键）落盘；**强制工具** `check_leakage`——机械检查（重复行跨界、标识列跨界、时序顺序、切分元数据存在），错了就拒绝，不给假分数。

### 第 5 步 数据预处理

- **主要问题**：让数据适配算法——转换（编码、分段/binning、归一化）、降维（特征选择、PCA）。每个变换都是一次"从数据里学参数"（均值、min/max、主成分、编码表），**这些参数只能从训练集学，然后应用到 test**。
- **人机分工**：变换本身 agent 执行；拟合边界由工具强制。
- **普通 agent 的失败**：PCA 用全量数据拟合（泄漏）；用 test 表现做特征选择（泄漏）。
- **机制**：`check_leakage` 验证拟合边界 + Pipeline 模板（让 fit/apply 的边界是结构性的，不是自觉）。

### 第 6 步 模型构建

- **主要问题**：**基线先行、公平对比、复杂度纪律**。先一个平凡基线（多数类预测/均值预测），再 2-3 个合理备选；备选只在估计器上不同，数据所见完全一样；对比基线：复杂模型只好在基线 0.5%，可能不值得部署。
- **人机分工**：agent 全权执行。
- **普通 agent 的失败**：跳过基线直接上复杂模型，然后"得分高 = 成功"（高得毫无意义）；对比不公平（换模型的同时也换了数据处理）。
- **机制**：流程技能把"先基线后复杂"设为协议。

### 第 7 步 结果验证与优化

- **主要问题**：正确评估 + 与基线对比 + 谨慎优化。指标和切分策略在第 1 步和第 4 步（切分）就定了，这一步是**验证**而不是"选个好看的数字"；测试集只碰一次；优化全程记录（试过什么、结果如何）。
- **人机分工**：全流程**最重的一次人工介入**——评估数字 agent 算，但"结果够不够好、要不要继续、什么时候停"是业务判断（成本结构/预算/期限），人拍板。
- **普通 agent 的失败**：报一个数字就收工；用 test 表现调参（测试集泄漏，最常见死法）；黑盒乱试不记录。
- **机制**：**评估报告工具化**（指标 + 基线对比 + 泄漏自查 + 试验历史，作为人把关的工件）+ **测试集纪律**协议（谁用 test 表现做选择就违反协议）。

### 第 8 步 知识应用与反馈

- **主要问题**：交付物 + 闭环。可理解的结论（什么驱动目标、哪些样本会错、适用边界），不只是模型分数；可复现的交付（报告/代码/模型文件/数据版本/切分元数据/seed）；反馈闭环（结果上线后真实表现 → 绕回第 1 步，下一轮挖掘的依据）。
- **人机分工**：结论呈现和代码交付 agent 做；"结果业务上能不能用"（够不够好、要不要上线、风险能否接受）人验收。
- **普通 agent 的失败**：只报分数就结束；报告不可复现（没记版本/seed/切分）；一次性交付，没有下一轮的起点。
- **机制**：报告模板（结论/性能/局限/复现/下一轮建议）+ manifest 提供可复现所需的一切。**第 8 步的"可复现"是前面所有"记录"的兑现**。

## 五、机制总览（三类 + 一个存储）

| 机制 | 大白话 | 动机 | 包含 |
|---|---|---|---|
| 看数据的工具 | agent 看不见数据，工具是它的眼睛 | 保真优先，给解释不给裸数字 | 盘点（discover）、profile 升级（真实统计/日期推断/解释性）、值分布（value_counts）、抽样（sample_rows 升级） |
| 卡错误的工具 | 机械可判定的对错，靠自觉必翻车 | 错了就拒绝，不给假分数 | 切分（split_dataset）、泄漏检查（check_leakage） |
| 流程约定 | 什么时候停下来问人、先干什么、记录什么 | 把专家的自我复核外化成协议 | 最小问题集、检查点、基线先行、测试集纪律、报告模板 |
| 存储（manifest） | 所有决定的落盘处 | 跨步骤传导 + 最终可复现 | 目标/边界/决策/切分元数据/试验历史 |

## 六、几条重要决定（讨论确认过的）

1. **协作定位**：agent 在每个阶段产出可审查的工件，人在四个节点介入——第 1 步（定目标/指标/边界）、第 2 步（补业务含义/聚合键/预测时点）、第 7 步（评估后拍板）、第 8 步（最终验收）。agent 未经确认不得进入下一阶段。
2. **先深后宽**：先把 CSV 表格数据做到极致（解析鲁棒、统计正确、防泄漏、评估报告），再谈 parquet/excel/jsonl（排到很后面）。
3. **工具级强制，先机械检查**：`split_dataset` + `check_leakage` 这种机械可判定的先做扎实；对 agent 脚本做静态分析（validate_pipeline）明确延后，留作探索。
4. **本仓库是唯一本体**：这个 bundle 是产品的 canonical 副本，harness 仓库里的 example/tool-data-profile 作为参考废弃，不再同步。
5. **数据清洗不含业务规则**：清洗 = 纯统计信号处理；刷单/测试数据那类业务规则不属于本插件范围。

## 七、明确不做的事（边界）

- 不做"帮你收集数据"的厚工具（join/聚合脚本交给 agent 用 bash 写）。
- 不做 validate_pipeline 脚本静态分析（初期）。
- 不做全自主"跑完直接交报告"（协作定位的反面）。
- 不铺多格式（排后）。
- 不把"通用数据挖掘知识"当插件卖点（模型都会，低价值）；技能的价值在协议成分（何时问人、何时停、记录什么），不在知识成分。

## 八、路线图

- **Phase 0 地基**：CSV 解析鲁棒性（BOM/编码/分隔符/大小上限/采样）+ 真实数值统计 + 日期推断，配对拍测试。
- **Phase 1 感知补全**：值分布（value_counts）、盘点（discover_datasets）、抽样升级（列过滤/条件采样）。
- **Phase 2 守门**：切分（split_dataset）+ 泄漏检查（check_leakage）+ 检查点协议固化进人设/技能。
- **Phase 3 流程**：manifest + 报告模板 + 多 agent 协作。
- **Phase 4 扩展**：多格式/规模化。

## 九、验证方式

- 小基准集（2-4 个有 ground truth 的数据集：churn、房价、titanic 级），跑"有插件 vs 无插件"对比任务完成度。
- 协作检查点本身是评估项：agent 是否在正确的节点停下来汇报、人能否看懂并能否决。

## 十、还没想清楚/待验证的问题

1. **manifest 会不会变成摆设？** agent 有了外部记忆，但会不会真的读它、维护它？设计上可以迫使（阶段 check-in/check-out 必须读写），行为是否成立要实验验证。
2. **"何时问人、问几个"能做成协议吗？** 问多了用户烦，问少了假设漂移。这是最微妙、最值得实验的部分。
3. **注意力分配能强制吗？** 工具能"鼓励"定向（profile 把可疑项排名），但没法强迫 agent 只看该看的，程度待测。
4. **处理策略的确认会不会太重？** 第 4 步的统计决策（drop/impute 的每个选择）都要人确认可能太烦。备选：只对影响大的决策走确认，机械的自决并记录。
5. **数据收集的粒度对齐怎么自动化？** join 键、聚合规则的边界声明靠协议，但"发现三张表可以 join"这件事 agent 能不能可靠地自己发现，待验证。
