# PHASE-GATE-DESIGN.md — 阶段门禁改进设计（v2 提案）

> **定位**：在 `DESIGN.md`（基线）之上的改进提案。基线定的是"做什么、为什么"，本文档定的是"把检查点从'靠自觉'升级为'靠结构'"的具体设计。按基线规矩：改 `DESIGN.md` 本身需讨论；本文档是提案，评审通过后并入基线（建议合并到基线的第五节机制总览与第六节决定）。
>
> **触发事件**：一次真实任务（对公贷款逾期预测）中，agent 在目标未确认时就看了数据、评估后未询问"继续优化还是交付"就交付，四个检查点全部跳过——即使它们明确写在 persona 与技能里。这证明**文本协议无法保证流程纪律**。

---

## 一、问题诊断：检查点为什么会被跳过

| 现象 | 根因 |
|---|---|
| 检查点写在 persona/技能里，agent 还是跳过 | **软约束**：执行与否取决于模型是否"记得"并"愿意"遵守；长任务、上下文被工具结果冲刷时，模型倾向"高效地一路冲到底" |
| 工具（split/check_leakage）从不被跳过 | **硬约束**：工具不存在/不可调用时模型无法使用它；机械可判定对错，错了直接拒绝 |
| agent 会"看起来正常"地推进 | 没有外部状态记录"当前在哪个阶段、哪个已确认"，模型只凭上下文自认为进度 |

**结论**：改进方向不是写更多提示词，而是把"阶段推进"变成**模型无法自行决定的事**——这正是基线 DESIGN.md 待验证问题 #1（manifest 会不会变摆设）、#2（何时问人能否成协议）的答案：**由状态机 + 工具 + 用户界面三方共同强制，而不是由模型自觉**。

---

## 二、改进原则（评审点）

1. **结构强制优先于自觉**：机械可判定的流程节点（何时能看数据、何时能切分、何时算完成）用状态/工具/权限卡死；提示词只做告知，不做保障。
2. **三角色分离**：**模型提议**（"我认为这步做完了"）、**系统核验**（退出条件机器可查）、**用户放行**（点确认/否决/强制通过）。没有任何一方能独自推进流程。
3. **讨论与执行正交**：
   - **讨论无门禁**：所有阶段随时可进入、可提问、可谈方案——对话文本不设任何限制；
   - **执行有门禁**：同一时间只有一个"当前阶段"允许真干活（调工具、写文件）；其余阶段可聊不可做。
   - 二者天然可分：门禁只管工具与文件写入，不管对话内容。
4. **双签完成**：阶段标记 done 需同时满足——agent 调 `dm_complete` 提交 **+** 系统核验退出条件通过 **+** 用户在 UI 点"确认"。模型不能自说自话宣布完成。
5. **前向严格、后向自由**：不完成前置阶段，后续阶段不可执行；已完成的阶段可"重做"回退，其后所有阶段重新锁定。
6. **人类判断优先**：提供"强制通过"——核验不过但用户知情同意时放行。故意设计成**显式的、有意识的动作**（而非默认路径），让跳步成为决策而不是默认。

---

## 三、阶段模型：用户可见 7 框 ↔ 内部 manifest 枚举

内部状态**沿用 manifest 现有的 10 阶段枚举**（`business → data-understanding → data-collection → data-cleaning → split → preprocessing → modeling → evaluation → deployment → done`），不发明第二套阶段名；用户可见的 7 框是它的聚合视图（`data-collection` 并入②，`preprocessing` 拆入③④）。

| # | 用户框 | 内部阶段映射 | 对应基线 8 步 | 主要产出（退出条件核验对象） |
|---|---|---|---|---|
| ① | 业务理解与目标确认 | `business` | 第1步 | manifest `set_goal`（目标/标签/指标）+ 用户确认记录 |
| ② | 数据理解与质量评估 | `data-understanding`(+`data-collection`) | 第2、3步 | 每表 `add_dataset` 条目（数据地图/疑点/业务含义）+ 质量评估报告 |
| ③ | 数据清洗与防泄漏拆分 | `data-cleaning` + `split` | 第4步+切分 | 清洗决策清单 + `split.json` + `check_leakage` 通过 |
| ④ | 特征工程 | `preprocessing` | 第5步 | 特征矩阵 + 特征清单 + 拟合边界说明 |
| ⑤ | 建模 | `modeling` | 第6步 | 基线 + 备选模型文件 + 训练日志 |
| ⑥ | 评估与交付决策 | `evaluation` | 第7步 | 指标 + 基线对比 + `check_leakage` 结果 + **用户决策（继续优化/交付）** |
| ⑦ | 交付报告 | `deployment`+`done` | 第8步 | 最终报告（引用 manifest 的目标/决策/切分） |

> 与基线第六节第 1 条的关系：基线定"人在四个节点介入（第1/2/7/8步）"。本设计把**七个框同等对待**：每个框的完成都是同一套"双签"流程（agent 提交 + 系统核验 + 用户确认），不区分轻重。基线的四个介入节点因此被扩展为全程统一的门禁；①②⑥⑦ 天然是人工节点，③④⑤ 也不例外。**决策：各步骤同等对待，不做轻重区分。**

---

## 四、状态模型

### 4.1 每个阶段两个正交状态

```
enterable（可进入/可讨论）= 全部默认 true      → 并行，UI 自由导航
executable（可执行）      = 仅当前阶段 true     → 顺序，硬门禁
done（完成）              = 双签达成            → 解锁下一个阶段
```

### 4.2 manifest 扩展（与现有 `phase` 字段兼容）

```jsonc
// dsh_manifest/manifest.json 现有结构上新增：
"phase": "modeling",                 // 保留：当前可执行阶段（兼容现有读取方）
"phaseGates": {                      // 新增：逐阶段状态
  "business":           { "status": "done",      "confirmedBy": "user", "confirmedAt": "..." },
  "data-understanding": { "status": "done",      "confirmedBy": "user" },
  "data-cleaning":      { "status": "unlocked",  "verified": { "split.json": true, "check_leakage": true } },
  "split":              { "status": "locked" },
  "preprocessing":      { "status": "locked" },
  "modeling":           { "status": "locked" },
  "evaluation":         { "status": "locked" },
  "deployment":         { "status": "locked" },
  "done":               { "status": "locked" }
}
// status: locked | unlocked(=可执行) | done
// 规则：unlocked 阶段至多一个；done 的前置才能 unlock 下一个
```

- 迁移兼容：旧会话无 `phaseGates` 时，按现有 `phase` 值推断（该阶段 unlocked，其余 locked），不破坏旧流程。
- 回退：用户点"重做"→ 该阶段置 `unlocked`，其后全部 `locked`，`phase` 同步回退。

### 4.3 状态转换（唯一状态机）

```
locked --(前置阶段 done)----------> unlocked
unlocked --(agent提交 + 系统核验通过 + 用户确认)--> done
done --(用户点"重做")-------------> unlocked（其后全部回到 locked）
unlocked --(用户点"强制通过")-----> done（记录 override 原因）
```

---

## 五、交互逻辑（用户视角，评审点）

### 5.1 面板初始状态示例

```
① 业务理解与目标确认   [✅ done]      ← 可讨论 / 可重做
② 数据理解与质量评估   [🔓 可执行]    ← 当前唯一可执行
③ 数据清洗与防泄漏拆分 [🔒 仅讨论]
④ 特征工程             [🔒 仅讨论]
⑤ 建模                 [🔒 仅讨论]
⑥ 评估与交付决策       [🔒 仅讨论]
⑦ 交付报告             [🔒 仅讨论]
```

### 5.2 用户五种操作

| 操作 | 效果 |
|---|---|
| 点任意框 | 打开面板：说明、产出要求、当前产物清单、讨论入口（永远可用） |
| 输入提问/指示 | agent 在当前可执行框干活；对任何框的提问只讨论不执行（用户自己界定） |
| 点"确认完成" | 系统先核验退出条件，通过才生效；不通过则拒绝并列出缺什么 |
| 点"重做" | 该框及其后全部重新锁定，回退重来 |
| 点"强制通过" | 跳过核验直接完成，须填写原因（人类判断优先） |

### 5.3 agent 四种操作

1. 在当前 🔓 框内正常调工具、写文件；
2. 任何框纯文本讨论（无执行效果）；
3. 调 `dm_complete <phase>` 提交完成——**只是申请，不是结论**；
4. 越权尝试（对 🔒 框调工具 / 写越级脚本）→ 被拒绝，返回"🔒 该阶段未解锁，可讨论不可执行"，且不产生实际效果。

### 5.4 边界情况既定行为

- **用户一直不确认**：流程停在当前框；agent 只能完善当前框或等待。
- **模型偷跑**（未确认就写下一阶段的脚本）：写入门禁拒绝 + 提示；用户在场时弹审批由其决定（见第六节写入门禁）。
- **用户跳步**（"直接建模"）：技术上锁着，必须逐个"强制通过"——故意设计成麻烦，让跳步成为有意识决策。
- **讨论中混入执行**：agent 检查目标阶段是否 🔓，未解锁则回复"只能先讨论"。
- **框⑥ 内容特殊、流程同等**：agent 提交评估后，UI 弹出决策卡片（指标+基线对比+泄漏自查+两个按钮"继续优化/交付"）；"继续优化"自动回退框⑤。确认动作与其他框一样由用户点"确认完成"。

---

## 六、实现架构（映射到 DSH 机制，均已核实源码位置）

### 6.1 宿主插件（新增，走 cordis 插件行）

| 能力 | DSH 机制 | 依据 |
|---|---|---|
| 注册 `dm_complete` / `dm_phase` 工具 | `ctx.tools.register(defineTool(...))` | 现有 `tool-ask-user`、plan-mode 同款 API |
| **阻塞式等用户确认** | 工具实现里调用 `ctx.userQuestions`（`ask_user_question` 的底层服务）——"工具暂停，直到 UI 返回人类答案才继续，答案作为工具结果喂回 agent 循环" | `packages/interaction/tool-ask-user/src/index.ts` 注释 |
| 每次请求注入"各阶段状态" | `systemPrompt.section()` 提供 `dm:phases` 段（当前可执行范围+各框状态+禁用提示） | `packages/core/system-prompt/src/index.ts:381` |
| 按阶段过滤工具目录 | `systemPrompt.tools()` 按请求组装；**能否"移除"已注册工具需验证**（见第十节风险1） | `packages/core/system-prompt/src/index.ts:430` |
| 状态读写 | 直接读写 `dsh_manifest/manifest.json`（现成 ledger，插件已有 `manifest.ts`） | 本仓库 `src/manifest.ts` |

### 6.2 客户端插件（新增 UI 包，浏览器半体）

| 能力 | DSH 机制 | 依据 |
|---|---|---|
| 任务框 stepper 面板 | 新包声明 `exports["./client"]`（`dsh.client`），`ctx.slots.inject('<槽位>', ...)` 挂 React 组件 | 参考 `ui-plan`、`ui-goal`、**`ui-workflow-run`（最接近的模板）** |
| 用户点按钮 → 通知宿主 | `ctx.remote.commands.execute(sessionId, '/dm complete ...')` 执行宿主命令 | `packages/client/ui-plan/src/client/index.ts` 同款 |
| 读取阶段状态 | 通过宿主命令/接口查询 manifest，或 `useProjection`（若后续做成 session projection） | plan-mode 的 projection 模式 |
| 展示"退出条件核验" | 面板调宿主核验接口，逐项打勾（`split.json ✓ / check_leakage ✓ / 重复行 0 ✓`），全绿才亮"确认完成" | — |

### 6.3 写入门禁（防模型用 bash 绕过，最重要的一条）

- **兜底（零开发，立即可用）**：sandbox 已有审批机制——越权写入触发 `sandbox_permissions` 升级弹窗，用户在弹窗里决定。未解锁阶段的脚本写入会以"审批弹窗"形式回到用户手上，天然是人工闸门。
- **增强（待验证）**：能否让插件按 `phaseGates` 动态贡献 sandbox 规则（如 `scripts/03_*.py` 仅在 `preprocessing` 解锁后可写）——需查 `packages/sandbox/sandbox-policy` 的插件扩展接口（见第十节风险2）。可做则自动拒绝+提示，不可做则维持审批弹窗兜底。

### 6.4 与 goal 自动延续的冲突（必须一起设计）

goal 的自动多轮续跑会绕过 UI 门禁自主推进（agent 在用户点"下一步"前就进入下一阶段）。**方案**：门禁启用期间，将"阶段推进"设为自动延续的暂停条件——即自动续跑只允许在当前可执行阶段内继续，阶段切换必须回到用户确认；或门禁开启时停用自动延续。

---

## 七、退出条件定义表（机器核验，评审点）

| 框 | 自动核验条件 | 数据来源 |
|---|---|---|
| ① 业务理解 | `set_goal` 存在 且 用户确认记录存在 | manifest |
| ② 数据理解 | 每张已入工作区的数据表都有 `add_dataset` 条目 | manifest |
| ③ 清洗与拆分 | `split.json` 存在 且 `check_leakage` 通过（重复行0/标识列不跨界/时序顺序） | split 元数据 + 机械检查 |
| ④ 特征工程 | 特征矩阵文件存在 且 特征清单文件存在 且 无"全量拟合"标记 | reports/ 产物 |
| ⑤ 建模 | `models/*.joblib` 存在 且 训练日志记录基线+备选 | models/ + 日志 |
| ⑥ 评估 | 指标文件存在 且 含基线对比 且 `check_leakage` 结果被引用 且 用户已决策（继续优化/交付） | reports/ + manifest |
| ⑦ 交付 | 报告文件存在 且 报告文本引用 manifest 目标/决策/切分 | reports/ + manifest |

> 设计取向：所有框统一执行"系统自动核验退出条件 → 打勾展示 → 用户确认"。核验自动化减轻的是**审阅负担**（用户不用逐项核对产物，看红灯即可），但**确认动作本身对七框一视同仁、不省略**（对应"各步骤同等对待"的决定）。这回应基线待验证问题 #4：负担的解法是"自动核验帮用户看清缺什么"，而不是"免掉用户的确认"。

---

## 八、实现清单与排期（评审点）

### Route A：宿主门禁最小闭环（半天~1天）——先做，验证强制力
1. `manifest.ts` 扩展 `phaseGates` 读写 + 状态机（locked/unlocked/done + 转换规则 + 旧会话兼容推断）；
2. 宿主工具 `dm_complete <phase>`（阻塞式：调 `userQuestions` 弹确认，核验退出条件，通过才置 done 并解锁下一阶段）+ `dm_phase`（查询）；
3. `dm:phases` prompt section（每次请求注入各框状态）；
4. persona 检查点文字更新为"阶段由系统门禁控制，调用 `dm_complete` 前不得越权执行"。
- **验收**：跑一个小任务，验证①agent 无法在未确认时执行下一阶段工具/写越级脚本（被拒）；②用户确认后解锁正常；③旧会话不破坏。

### Route B：客户端 stepper 面板（1~2天）
5. 新 client 包（`ui-data-mining`）：7 框 stepper + 退出条件打勾 + 确认/重做/强制通过按钮 + 框⑥决策卡片；
6. 槽位选择：侧边栏面板或独立 tab（以 `ui-workflow-run` 为模板）；
7. 按钮 → `remote.commands.execute('/dm ...')` 闭环。
- **验收**：全流程用户只点按钮不写命令可完成 ①→⑦；按钮在条件不满足时置灰。

### Route C：写入门禁增强（待验证后做）
8. 查 `sandbox-policy` 扩展接口；可行则按阶段贡献写入规则；不可行则维持审批弹窗兜底并在文档注明。

---

## 九、验证方式（对齐基线第九节）

- 基准集（churn/titanic 级）跑"有门禁 vs 无门禁（现版本）"对比：
  - **检查点命中率**：目标未确认前是否 0 次数据工具调用（门禁版应=0）；
  - **越权拦截率**：agent 尝试跳步/偷跑被拒的次数与处理；
  - **完成率与质量**：同基线（数据地图/切分元数据/报告可复现性不下降）；
  - **用户负担**：手动确认次数、每次确认耗时（验证统一确认流程下负担是否可接受，及 UI 缓解手段是否有效）。
- 回归：现有 profile/split/check_leakage/manifest 工具行为不变。

---

## 十、待验证与风险（评审点）

1. **工具目录过滤**：`systemPrompt.tools()` 是"贡献"工具，能否在组装时**移除**已注册工具？若不能，门禁靠"工具内拒绝"（调用时查 `phaseGates` 返回拒绝）实现——两种都可行，需定一个。
2. **sandbox-policy 插件扩展性**：未解锁阶段的文件写入能否插件化控制，还是只能靠审批弹窗兜底。
3. **用户负担**：七框统一确认可能比"只盯四个节点"更重——这是"各步骤同等对待"决定的取舍，已接受；缓解靠 UI（确认按钮一键化、退出条件自动打勾、核验不过直接显示缺什么），不靠跳过确认。若实测负担过重，再回到基线讨论。
4. **与 goal 自动延续的冲突**：门禁启用时暂停自动多轮或限定在当前阶段内续跑，否则门禁失效（见 6.4）。
5. **UI 热更新**：client 插件改动需 `pnpm run dev:web` 才热更新；发布需重建 web 产物并刷新页面。
6. **多会话/多工作区**：`phaseGates` 存 manifest（工作区级），天然随工作区隔离；若需会话级状态再考虑 session projection。

---

## 十一、与 DESIGN.md 的关系（评审点）

- **不推翻基线**：基线的"人定方向、agent 干活""机械环节用工具卡死""所有决定记下来"三条核心不变；
- **升级一点**：基线第五节"机制总览"的"流程约定"一行，从**纯文本协议**升级为**结构门禁**（状态机 + 阻塞工具 + UI 确认 + 写入门禁兜底）；
- **回应基线待验证问题**：#1（manifest 变摆设）→ 阶段状态机强制读写；#2（何时问人）→ 7 框手动推进 + 自动核验；#4（确认太重）→ 各步同等对待：自动核验降审阅负担，确认动作不省略；
- **检查点升级**：基线第六节第 1 条的四个介入节点（第1/2/7/8步 = 框①②⑥⑦）保留（这些节点人工输入内容关键），且门禁扩展到全部 7 框——各步同等对待、统一双签。
